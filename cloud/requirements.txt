streamlit==1.58.0
streamlit-image-coordinates==0.4.0
Authlib>=1.3.2
# Authlib's Starlette integration (used by st.login) imports httpx at runtime,
# but neither streamlit[auth] nor Authlib declares it. Without it the
# /auth/login route fails with a misleading StreamlitMissingAuthlibError.
httpx>=0.24,<1
cryptography>=42,<50
google-api-python-client>=2.130
google-auth>=2.29
pypdf>=4.2
requests>=2.32
websockets>=12
# Image-only invoices carry their amount only in the picture. This engine
# needs no system package, so it works on Streamlit Cloud as-is.
rapidocr-onnxruntime>=1.3
pillow>=10
numpy>=1.26
